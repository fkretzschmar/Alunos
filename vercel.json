{
    "version": 2,
    "builds": [
        {
            "src": "start.ts",
            "use": "@vercel/node --loader ts-node/esm --inspect"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.ts"
        }
    ]
}